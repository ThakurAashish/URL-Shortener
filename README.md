# URL-Shortener
This Python program implements a simple URL Shortener, which converts a long URL into a short, unique URL code and allows retrieving the original URL later. It demonstrates the basic working principle behind services like bit.ly or tinyurl.
